name: my-test
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps: # список шагов, которые надо выполнить
      - uses: actions/checkout@v3
      - run: make install
      - run: make lint
      - run: make test